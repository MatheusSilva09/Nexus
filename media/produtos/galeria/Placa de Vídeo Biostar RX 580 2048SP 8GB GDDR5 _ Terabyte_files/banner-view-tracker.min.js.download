!(function (e) {
  "use strict";
  var n = {},
    a = {};
  function t(t) {
    n[t] ||
      ((n[t] = !0),
      e.ajax({
        url: "/content/banner_view.php",
        method: "POST",
        data: { idbanner: t },
      }));
  }
  function r(e, r) {
    ((r = r || 3e3),
      a[e] && clearTimeout(a[e]),
      (a[e] = setTimeout(function () {
        (n[e] || t(e), delete a[e]);
      }, r)));
  }
  function o() {
    var o = [
        ".f__banner .carousel-inner .item a.banner-link[data-banner-id]",
        ".f__banner a.banner-link[data-banner-id]",
        "a.banner-link[data-banner-id]",
        ".banner-animado a.banner-link[data-banner-id]",
        ".banner-promocional a.banner-link[data-banner-id]",
        ".swiper-slide a.banner-link[data-banner-id]",
        ".carousel-inner a[data-banner-id]",
      ],
      i = [];
    if (
      (o.forEach(function (e) {
        var n = document.querySelectorAll(e);
        n.length > 0 && (i = i.concat(Array.prototype.slice.call(n)));
      }),
      0 ===
        (i = (function (e) {
          var n = [],
            a = {};
          return (
            e.forEach(function (e) {
              var t = e.getAttribute("data-banner-id");
              t && !a[t] && ((a[t] = !0), n.push(e));
            }),
            n
          );
        })(i)).length)
    )
      return;
    if (!("IntersectionObserver" in window))
      return (
        (function () {
          var a = e("a.banner-link[data-banner-id]");
          (a.each(function () {
            var a = e(this),
              r = a.data("banner-id");
            !(function (e) {
              var n = e.getBoundingClientRect();
              return (
                n.top >= 0 &&
                n.left >= 0 &&
                n.bottom <=
                  (window.innerHeight ||
                    document.documentElement.clientHeight) &&
                n.right <=
                  (window.innerWidth || document.documentElement.clientWidth)
              );
            })(a[0]) && t(r);
          }),
            e(window).on("scroll resize", function () {
              a.each(function () {
                var a = e(this),
                  r = a.data("banner-id");
                !n[r] &&
                  (function (e) {
                    var n = e.getBoundingClientRect();
                    return (
                      n.top >= 0 &&
                      n.left >= 0 &&
                      n.bottom <=
                        (window.innerHeight ||
                          document.documentElement.clientHeight) &&
                      n.right <=
                        (window.innerWidth ||
                          document.documentElement.clientWidth)
                    );
                  })(a[0]) &&
                  t(r);
              });
            }));
        })(),
        void 0
      );
    var c = new IntersectionObserver(
      function (e, n) {
        e.forEach(function (e) {
          if (e.isIntersecting) {
            var a = e.target.getAttribute("data-banner-id");
            a && (t(a), n.unobserve(e.target));
          }
        });
      },
      { root: null, rootMargin: "0px", threshold: 0.5 },
    );
    i.forEach(function (e) {
      c.observe(e);
    });
  }
  (e(document).ready(function () {
    (!(function () {
      var t = 0;
      !(function r() {
        (t++, o());
        var i = e("a.banner-link[data-banner-id]").length;
        0 === i && t < 5 ? setTimeout(r, 500) : i;
      })();
    })(),
      (function () {
        if ("undefined" != typeof MutationObserver) {
          new MutationObserver(function (n) {
            var a = !1;
            (n.forEach(function (n) {
              n.addedNodes.length > 0 &&
                n.addedNodes.forEach(function (n) {
                  1 === n.nodeType &&
                    (e(n).hasClass("banner-link") ||
                      e(n).find("a.banner-link[data-banner-id]").length > 0) &&
                    (a = !0);
                });
            }),
              a && o());
          }).observe(document.body, { childList: !0, subtree: !0 });
        }
      })(),
      e(document).on("swiper-init swiperInit", function () {
        setTimeout(function () {
          o();
        }, 100);
      }),
      (function () {
        function n(n) {
          var a = e(n).find(
            ".carousel-inner .item.active a.banner-link[data-banner-id]",
          );
          if (a.length > 0) {
            var t = a.data("banner-id");
            t && r(t, 2e3);
          }
        }
        (setTimeout(function () {
          e(".f__banner .carousel").each(function () {
            n(this);
          });
        }, 500),
          e(".carousel").on("slid.bs.carousel", function (e) {
            n(this);
          }),
          setInterval(function () {
            e(".f__banner .carousel").each(function () {
              var t = e(this);
              if (!t.hasClass("sliding")) {
                var o = t.find(
                  ".carousel-inner .item.active a.banner-link[data-banner-id]",
                );
                if (o.length > 0) {
                  var i = o.data("banner-id");
                  i && !n[i] && !a[i] && r(i, 2e3);
                }
              }
            });
          }, 3e3));
      })());
  }),
    e(window).on("load", function () {
      setTimeout(function () {
        o();
      }, 500);
    }),
    (window.reinitBannerViewTracker = function () {
      o();
    }));
})(jQuery);